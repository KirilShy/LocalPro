services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: localpro
      POSTGRES_USER: localpro
      POSTGRES_PASSWORD: localpro
    ports:
      - "5434:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data


volumes:
  postgres-data: